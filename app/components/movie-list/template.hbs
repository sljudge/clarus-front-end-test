<ul class={{this.styleNamespace}}>
  <MovieList::AddNewMovieCta @onClick={{@handleAddMovieClick}} />
  {{#each @movies as |movie|}}
    <MovieList::MovieListItem
      @movie={{movie}}
      @onClick={{@handleEditMovieClick}}
    />
  {{/each}}
</ul>